const initialState = {

}

export default function reducer(state = initialState, action) {
    // switch (action.type) {
    //     case value:
            
    //         return { ...state, };
    
    //     default:
    //         return state;
    // }
}